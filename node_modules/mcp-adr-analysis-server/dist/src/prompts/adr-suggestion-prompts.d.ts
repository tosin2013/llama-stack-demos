/**
 * AI prompts for ADR suggestion and implicit decision detection
 * Following prompt-driven development approach
 */
/**
 * AI prompt for detecting implicit architectural decisions by scanning project files
 */
export declare function generateImplicitDecisionDetectionPrompt(projectPath: string, existingAdrs?: string[]): string;
/**
 * AI prompt for analyzing significant code changes for decision points
 */
export declare function generateCodeChangeAnalysisPrompt(beforeCode: string, afterCode: string, changeDescription: string, commitMessages?: string[]): string;
/**
 * AI prompt for ADR template generation (Nygard, MADR formats)
 */
export declare function generateAdrTemplatePrompt(decisionData: {
    title: string;
    context: string;
    decision: string;
    consequences: string;
    alternatives?: string[];
    evidence?: string[];
}, templateFormat?: 'nygard' | 'madr' | 'custom', existingAdrs?: string[]): string;
//# sourceMappingURL=adr-suggestion-prompts.d.ts.map