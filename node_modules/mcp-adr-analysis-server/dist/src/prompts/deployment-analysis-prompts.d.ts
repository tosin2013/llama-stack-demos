/**
 * AI prompts for deployment completion analysis
 * Following prompt-driven development approach
 */
/**
 * AI prompt for identifying deployment tasks from ADRs
 */
export declare function generateDeploymentTaskIdentificationPrompt(adrFiles: Array<{
    id: string;
    title: string;
    content: string;
    status: string;
}>, todoContent?: string): string;
/**
 * AI prompt for analyzing CI/CD logs and status
 */
export declare function generateCiCdAnalysisPrompt(cicdLogs: string, pipelineConfig?: string, deploymentTasks?: Array<{
    taskId: string;
    taskName: string;
    category: string;
    verificationCriteria: string[];
}>): string;
/**
 * AI prompt for calculating deployment progress
 */
export declare function generateDeploymentProgressCalculationPrompt(deploymentTasks: Array<{
    taskId: string;
    taskName: string;
    status: string;
    progress: number;
    category: string;
    priority: string;
}>, cicdStatus?: any, environmentStatus?: any): string;
/**
 * AI prompt for verifying completion with outcome rules
 */
export declare function generateCompletionVerificationPrompt(deploymentTasks: Array<{
    taskId: string;
    taskName: string;
    verificationCriteria: string[];
    expectedOutcome: string;
    status: string;
}>, outcomeRules: Array<{
    ruleId: string;
    description: string;
    criteria: string[];
    verificationMethod: string;
}>, actualOutcomes?: Array<{
    taskId: string;
    outcome: string;
    evidence: string[];
    timestamp: string;
}>): string;
//# sourceMappingURL=deployment-analysis-prompts.d.ts.map