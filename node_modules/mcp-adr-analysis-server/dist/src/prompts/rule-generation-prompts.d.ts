/**
 * AI prompts for rule generation and validation
 * Following prompt-driven development approach
 */
/**
 * AI prompt for extracting actionable rules from ADRs
 */
export declare function generateRuleExtractionPrompt(adrFiles: Array<{
    id: string;
    title: string;
    content: string;
    status: string;
    category?: string;
}>, existingRules?: Array<{
    id: string;
    name: string;
    description: string;
}>): string;
/**
 * AI prompt for generating rules from code patterns
 */
export declare function generatePatternBasedRulePrompt(projectStructure: string, codePatterns: Array<{
    pattern: string;
    frequency: number;
    examples: string[];
    category: string;
}>, existingRules?: string[]): string;
/**
 * AI prompt for validating code against rules
 */
export declare function generateCodeValidationPrompt(codeToValidate: string, fileName: string, rules: Array<{
    id: string;
    name: string;
    description: string;
    pattern: string;
    severity: string;
    message: string;
}>, validationType?: 'file' | 'function' | 'component' | 'module'): string;
/**
 * AI prompt for reporting rule deviations and compliance status
 */
export declare function generateRuleDeviationReportPrompt(validationResults: Array<{
    fileName: string;
    violations: Array<{
        ruleId: string;
        ruleName: string;
        severity: string;
        message: string;
        location: any;
    }>;
    compliance: Array<{
        ruleId: string;
        ruleName: string;
        status: string;
    }>;
    overallCompliance: number;
}>, rules: Array<{
    id: string;
    name: string;
    category: string;
    severity: string;
}>, reportType?: 'summary' | 'detailed' | 'trend' | 'compliance'): string;
//# sourceMappingURL=rule-generation-prompts.d.ts.map