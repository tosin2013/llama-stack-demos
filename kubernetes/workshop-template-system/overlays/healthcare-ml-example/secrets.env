# Healthcare ML Workshop Secrets
# Copy this file and customize with your actual values

# RAG functionality uses local Milvus vector DB (no external API key needed)
# PINECONE_API_KEY=not-required-using-local-milvus

# Required: GitHub Token for repository access
GITHUB_TOKEN=demo-github-token-placeholder

# Optional: OpenAI API Key (NOT REQUIRED - system uses local Llama Stack)
# Only uncomment if you want to use external OpenAI features
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Required: Gitea Admin Token for repository management (will be created during deployment)
GITEA_ADMIN_TOKEN=will-be-created-during-deployment

# Required: Webhook Secret for BuildConfig triggers
WEBHOOK_SECRET=healthcare-ml-webhook-secret-1735353600

# Healthcare ML Specific Configuration
ML_MODEL_REGISTRY=your-model-registry-url
KAFKA_BOOTSTRAP_SERVERS=your-kafka-servers
QUARKUS_PROFILE=openshift
